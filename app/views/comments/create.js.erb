console.log()
$('<%="#post_#{@comment.commentable_id}_body" %>').val('');

var commentHtml = "<%= escape_javascript(render @comment) %>";

$('<%= "#post_#{@comment.commentable_id}_comments" %>').hide().fadeIn(1500).prepend(commentHtml);
